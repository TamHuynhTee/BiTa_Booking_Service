import React from 'react';
import { useRouteMatch } from 'react-router';
import { Link } from 'react-router-dom';

interface Props {}

export const BranchList = (props: Props) => {
    return <div>branch List</div>;
};
